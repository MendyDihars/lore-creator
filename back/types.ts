export interface ILore {
  id: string;
  name: string;
}

export interface IEvent {
  id: string;
  name: string;
  image?: string;
}

export interface IPeriod {
  id: string;
  name: string;
  position: number;
}